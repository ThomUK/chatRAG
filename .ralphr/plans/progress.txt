## Issue #2: Slice 1 — Project Foundation
Status: COMPLETE (2026-04-11)

### Changes made:
- DESCRIPTION: Added all required imports (bslib, dplyr, DT, httr2, ollamar, pdftools, readr, stringr) alongside existing config, golem, shiny
- inst/app/data/pdfs/.gitkeep: Created directory with gitkeep
- inst/app/data/documents.csv: Created placeholder with correct columns (title, organisation, date, url, filename)
- .gitignore: Added .Renviron, inst/app/data/embeddings.rds, inst/app/data/pdfs/
- .Renviron.example: Created with placeholders for ANTHROPIC_API_KEY, OPENAI_API_KEY, AZURE_OPENAI_KEY, AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_DEPLOYMENT
- inst/golem-config.yml: Added all 5 API key env vars via !expr Sys.getenv(...)
- R/app_config.R: Added get_api_key(provider = "anthropic") helper
- tests/testthat/test-app_config.R: TDD tests for get_api_key() — all 4 tests pass

### TDD result: FAIL 0 | WARN 0 | SKIP 0 | PASS 4

## Issue #3: Slice 2 — fct_embeddings
Status: COMPLETE (2026-04-11)

### Changes made:
- R/fct_embeddings.R: Implemented full embedding pipeline
  - chunk_text: character-based overlapping chunking with edge-case handling
  - parse_pdf: pdftools wrapper that concatenates pages
  - embed_chunks: calls Ollama nomic-embed-text, returns tibble with 6 columns
  - build_knowledge_base: orchestrates parse→chunk→embed for multiple PDFs
  - append_to_knowledge_base: adds one document to existing knowledge base
  - save_knowledge_base / load_knowledge_base: saveRDS/readRDS wrappers
  - check_ollama_running: pings localhost:11434, returns TRUE/FALSE
  - Internal wrappers (.call_pdf_text, .call_ollama_embed, .call_ollama_ping) for test mockability
- DESCRIPTION: moved pdftools from Imports to Suggests (requires libpoppler-cpp system lib)
- tests/testthat/test-fct_embeddings.R: 30 tests via TDD (red→green→refactor)

### TDD result: FAIL 0 | WARN 0 | SKIP 0 | PASS 30

## Issue #4: Slice 3 — fct_retrieval
Status: COMPLETE (2026-04-11)

### Changes made:
- R/fct_retrieval.R: Implemented retrieval layer
  - cosine_similarity: dot-product / product-of-norms, handles identical (1), orthogonal (0), opposite (-1)
  - embed_query: thin wrapper around .call_ollama_embed (reused from fct_embeddings), returns numeric vector
  - retrieve_chunks: vapply over knowledge_base$embedding, orders by similarity desc, slices top_n rows, drops embedding column, returns 6-column tibble
- tests/testthat/test-fct_retrieval.R: 22 tests via TDD (red→green)
  - cosine_similarity: 6 tests (scalar type, identical, orthogonal, opposite, symmetric, unit vector)
  - embed_query: 3 tests with local_mocked_bindings (type, length, query passthrough)
  - retrieve_chunks: 13 tests with synthetic embedding matrices (data frame, top_n rows, graceful underflow, required columns, no embedding column, ordered desc, score==1 for best, numeric scores, default top_n=5, single-row KB)

### TDD result: FAIL 0 | WARN 0 | SKIP 0 | PASS 22

## Issue #5: Slice 4 — fct_llm
Status: COMPLETE (2026-04-11)

### Changes made:
- R/fct_llm.R: Implemented full LLM integration layer
  - build_system_prompt: fixed system prompt (cite sources, disclose fallback, boardroom tone)
  - build_messages: 6-turn rolling window, context injection, unified {system, messages} payload
  - call_llm: dispatches to Claude/OpenAI/Azure; streaming insertion point clearly marked
  - test_connection: ping with minimal prompt, returns {success, message}
  - Internal wrappers (.call_claude_api, .call_openai_api, .call_azure_api) for test mockability
- tests/testthat/test-fct_llm.R: 43 tests via TDD (red→green)

### TDD result: FAIL 0 | WARN 0 | SKIP 0 | PASS 43

## Issue #6: Slice 5 — Welcome / Setup Screen
Status: COMPLETE (2026-04-11)

### Changes made:
- R/fct_welcome.R: Implemented all welcome-screen business logic
  - .list_pdf_files, .read_documents_csv, .check_file_exists: filesystem wrappers for mockability
  - check_ollama_status(): delegates to check_ollama_running(), returns {ok, message}
  - check_pdfs_status(pdf_dir): checks for ≥1 .pdf in pdfs directory
  - check_documents_csv_status(csv_path): checks file exists, is parseable, and has required columns
  - run_prerequisite_checks(pdf_dir, csv_path): orchestrates all three checks
  - all_checks_pass(checks): returns TRUE only when all three checks ok=TRUE
  - embeddings_file_exists(rds_path): thin wrapper around .check_file_exists
- R/app_ui.R: Replaced golem welcome page with bslib::page_fluid + uiOutput("main_content")
  - prerequisite_row(): renders a single check row with pass/fail styling
  - welcome_screen_ui(checks): renders the full welcome card; build button disabled until all pass
  - main_tabbed_ui(): placeholder 3-tab UI (Source Material, Chat, Connect Your Own)
- R/app_server.R: Full welcome screen server logic
  - kb_ready reactiveVal seeded from embeddings_file_exists() at startup
  - reactiveTimer(3000) refreshes prerequisite checks every 3 s while on welcome screen
  - output$main_content switches between welcome_screen_ui and main_tabbed_ui
  - observeEvent(input$build_kb): runs build_knowledge_base → save_knowledge_base, shows inline
    progress messages ("Parsing PDFs…", "Creating embeddings…", "Updating knowledge base…"),
    transitions to main UI on success, shows error on failure
- tests/testthat/test-fct_welcome.R: 38 tests via TDD (red→green)

### TDD result: FAIL 0 | WARN 0 | SKIP 0 | PASS 38 (137 total across all test files)

## Issue #7: Slice 6 — App Shell + Source Material Tab
Status: COMPLETE (2026-04-11)

### Changes made:
- R/fct_source_material.R: Implemented source material business logic
  - load_documents_csv(csv_path): reads documents.csv via readr, returns tibble with 5 columns
  - prepare_source_table(documents): renames display columns, builds HTML anchor for Source, drops internal columns
- R/app_ui.R:
  - main_tabbed_ui(): replaced placeholder Source Material tab with source_material_tab_ui()
  - source_material_tab_ui(): intro paragraph + DT::DTOutput("source_table") + "Add to Knowledge Base" button in top-right
  - mobile-warning banner via CSS class (shown below 1024px via media query)
  - golem_add_external_resources(): added Inter font preconnect + stylesheet <link> tags
- R/app_server.R: Added output$source_table renderDT block
  - Loads documents.csv via load_documents_csv()
  - Prepares table via prepare_source_table()
  - DT::datatable with escape=FALSE (for HTML links), sortable, no row names
- inst/app/www/app.css: Stripe-dashboard aesthetic
  - Inter font, off-white background (#f6f9fc), muted accents (#635bff violet)
  - Custom nav-tabs, card, button, and DataTable styles
  - Mobile warning block shown via @media (max-width: 1023px)
- tests/testthat/test-fct_source_material.R: 20 tests via TDD (red→green)

### TDD result: FAIL 0 | WARN 0 | SKIP 0 | PASS 20 (157 total across all test files)

## Issue #8: Slice 7 — Upload Modal
Status: COMPLETE (2026-04-11)

### Changes made:
- R/fct_upload_modal.R: Implemented upload business logic
  - validate_upload_inputs(file, title, org, date, url): validates all form fields — returns {ok, message}; checks for NULL file, non-PDF extension, trimmed empty strings
  - append_document_to_csv(csv_path, new_row): reads existing CSV, appends one row, writes back; returns csv_path invisibly
- R/app_ui.R:
  - upload_modal_ui(): modalDialog with fileInput(.pdf), textInputs for Title/Organisation/Date/URL, uiOutput("upload_progress") for progress bar
  - Added shinyjs::useShinyjs() to page_fluid
  - Added JS custom message handler "pulse_source_tab" that animates the Source Material nav-link
- R/app_server.R:
  - knowledge_base reactiveVal: loaded from RDS on startup, updated on upload
  - documents_rv reactiveVal: drives source_table reactively; updated after each successful upload
  - observeEvent(input$open_upload_modal): showModal(upload_modal_ui())
  - observeEvent(input$submit_upload): validates inputs → 3-step progress bar (33/66/90%) →
    append_to_knowledge_base → save_knowledge_base → append_document_to_csv → update reactives →
    session$sendCustomMessage("pulse_source_tab") → success alert → shinyjs::delay(1500, removeModal())
  - build_kb handler updated to populate knowledge_base reactiveVal
- inst/app/www/app.css: @keyframes tab-pulse-anim + .nav-link.tab-pulse class for subtle violet pulse
- DESCRIPTION: added shinyjs to Imports
- tests/testthat/test-fct_upload_modal.R: 29 tests via TDD (red→green)

### TDD result: FAIL 0 | WARN 0 | SKIP 0 | PASS 29 (186 total across all test files)
